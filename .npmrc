# Approve install scripts for Prisma + sharp + swc
# Tanpa ini, Prisma Client binary tidak ter-generate → error "Authentication failed"
allow-scripts=true
legacy-peer-deps=true
fund=false
audit=false
